# Python
.venv/
__pycache__/
*.pyc
# Secrets
.env
# Terraform (provider binaries + state — state can hold secrets)
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*
*.tfvars
# Lambda build artifacts (regenerated by archive_file on plan/apply)
control-plane.zip
# Node
node_modules/
.next/
# OS
.DS_Store
#docs
ARCHITECTURE.md
CLAUDE.md
